import { styled } from "styled-components";

const XContainer = styled.div`
  position: relative;
  margin-top: 2rem;
  margin-bottom: 2rem;
  margin-left: 30px;

  &::-webkit-scrollbar{
    display: none;
  }
`;

export default XContainer;